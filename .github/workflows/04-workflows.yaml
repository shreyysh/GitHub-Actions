name: 004-workflow-actions
on:
  push:

jobs:
  ubuntu_job:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: 04-using-actions
    steps:
      - name : list-files
        run: ls
name : first-workflow
on : push
jobs :
    first-job:
        runs-on: ubuntu-latest
        steps:
            - name : First-step
              run : echo "Welcome to github actions"
    second-job:
        runs-on: ubuntu-latest
        steps:
            - name : second-step
              run: |
               echo "Step failed"
               exit 1
            - name : third-step
              run: 
                echo "Finally step triggered"